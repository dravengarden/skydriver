# Carrack operator credentials. Copy this file to `.env`, set permissions to
# 0600, and fill in the values. The devShell exports it for Wrangler commands.
# These values authenticate the operator CLI; they are not Worker secrets.

CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
